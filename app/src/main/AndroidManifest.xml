<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.every">

    <application
        android:allowBackup="true"
        android:icon="@drawable/every_pets_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Every">
        <activity android:name=".EditInformacoesPet"></activity>
        <activity android:name=".CadastroComAnimalSalvo" />
        <activity android:name=".RegistrarPet" />
        <activity android:name=".Localizacao" />
        <activity android:name=".Cadastro" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>